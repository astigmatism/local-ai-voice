# TTS runtime

Create the production venv at:

```text
/opt/local-ai-voice/workers/tts/.venv
```

The source worker lives in `workers/tts-chatterbox`. See `scripts/setup-workers.sh` for idempotent venv creation and CUDA/PyTorch install notes.
