# STT runtime

Create the production venv at:

```text
/opt/local-ai-voice/workers/stt/.venv
```

The source worker lives in `workers/stt-fast-whisper`. See `scripts/setup-workers.sh` for idempotent venv creation.
